import PropTypes from 'prop-types';

const BookList = props => {
    return (
        <div>
            <h2 className="text-4xl">this is book list</h2>
        </div>
    );
};

BookList.propTypes = {
    props: PropTypes
};

export default BookList;